# M-Chose config:
  * 1. make sure both this config are used at the same time because there is a costume key 👇 that is set to change Layouts from default -> nixos. ⌨️
   > Fn + p 
  * 2. if we add another config here, make sure to set the same key for the same thing-> which is inside 
    - Key Maping -> (set FN1+p) -> Special(last symbol which looks like **Processor**)
## NIXOS: 
   * The NixOs setup is awesome.
   * We removed Apple switching Mapping -> Dont Have apple.
   * Removed "Windows key lock key" -> Ultimite Combi Key.
   * Keyboard features like lighting controls & keyboard Restore setting are on FN3 section 
   * FN3 is set to FN1 + ".>" button. (easy to press near button.
