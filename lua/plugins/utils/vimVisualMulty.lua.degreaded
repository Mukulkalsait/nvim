-- return {
--   -- Y: Multy
--   {
--     "mg979/vim-visual-multi",
--     branch = "master",
--     init = function()
--       -- Better compatibility with LazyVim and modern Neovim
--       vim.g.VM_default_mappings = 0 -- Disable default mappings to avoid conflicts
--
--       vim.g.VM_maps = {
--         ["Find Under"] = "<C-n>",
--         ["Find Subword Under"] = "<C-n>",
--         ["Select All"] = "<leader>a",
--         ["Start Regex Search"] = "<leader>/",
--         ["Add Cursor Down"] = "<C-down>",
--         ["Add Cursor Up"] = "<C-up>",
--         ["Select Cursor Down"] = "<C-S-down>",
--         ["Select Cursor Up"] = "<C-S-up>",
--         ["Visual Cursors"] = "<C-n>", -- Important for visual mode
--       }
--
--       -- Fix for visual/select mode bug
--       vim.g.VM_extend_mode = 1
--     end,
--     config = function()
--       -- Extra safety
--       vim.cmd([[
--         augroup visual_multi_fix
--           autocmd!
--           autocmd User visual_multi_mappings nmap <buffer> v <Plug>(VM-Visual-Cursors)
--         augroup END
--       ]])
--     end,
--   },
--   -- Y:  Ccc
--   { "ap/vim-css-color" },
-- }
